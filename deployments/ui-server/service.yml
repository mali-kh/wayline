apiVersion: v1
kind: Service
metadata:
  name: ui-server
  namespace: wl-system
  labels:
    app: ui-server
spec:
  type: NodePort
  selector:
    app: ui-server
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      nodePort: 30080
