# Build-host Python deps for models/fetch.sh.
#
# Install into a fresh venv on the build host. Use the pytorch CPU
# index to skip the multi-GB CUDA wheels — Intel iGPU via OpenVINO is
# the runtime target.
#
#   python3 -m venv .venv-build
#   source .venv-build/bin/activate
#   pip install --upgrade pip
#   pip install --index-url https://download.pytorch.org/whl/cpu \
#               --extra-index-url https://pypi.org/simple \
#               -r eval/videoedge-mcmt/models/requirements.txt
#   eval/videoedge-mcmt/models/fetch.sh
ultralytics==8.3.*
openvino==2024.4.*
torch==2.4.1+cpu
torchvision==0.19.1+cpu
numpy>=1.26,<2.1
opencv-python-headless==4.10.*
