"""
videoedge-mcmt computation library.

Pure functions only. No DSF SDK imports. No Argo conventions. Each module
takes paths to extracted input directories and produces files in an output
directory. The DSF and Argo wrappers handle tar.gz pack/unpack at the
stage boundary so the lib stays uniform.
"""
